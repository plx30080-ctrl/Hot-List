name = "employbridge-hotlist"
main = "worker.js"
compatibility_date = "2024-01-01"

# ANTHROPIC_API_KEY is stored as a secret (never in this file).
# Set it with: wrangler secret put ANTHROPIC_API_KEY
